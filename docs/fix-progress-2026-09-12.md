**Review fixes — September 12, 2026**

Fixes are implemented, verified, and committed individually in numbered finding order from the application review. Application data is not used for destructive testing.

1. **Complete — personalized hold probabilities.** Centralized current-profile/history calculation for search, detail, favorites, simulation/optimization, and coach reads. Probability filtering, ordering, and statistics follow calculation. Profile/history changes invalidate client data and isolate offline caches. Verified: two new regression tests, 245 bid-tool assertions, TypeScript, lint, client build, local browser startup, and API smoke. Live package 61 returns 90% at the 10th percentile versus 10% at the 90th percentile, with correctly ordered results.

The repository-wide formatting check had 71 pre-existing failing files in the audit. New modules/tests are formatted; existing files are kept focused rather than reformatted wholesale in each bug fix.

2. **Complete — distinct-pilot strategy ranks.** Deduplicated each period roster before computing percentiles. SQL uses the same cumulative roster convention as empirical hold evidence (including single-pilot periods). Verified with a read-only PostgreSQL fixture containing unequal preference counts, a populated live strategy query, 245 bid checks, TypeScript, lint, browser startup, and smoke.

3. **Complete — database recovery.** Publish a tested replacement through a live client binding, serialize reconnect attempts, close failed candidates, attach error handlers to every pool, and coordinate cleanup. Verified five regression tests including a real storage method under injected connection failure, concurrent recovery, and failed candidate cleanup; 245 bid checks; TypeScript; lint; local browser startup; and smoke.

4. **Complete — atomic, truthful Reasons imports.** Stage parsed records, then commit awards and preference replacement together. Propagate preparation/insertion failures and count committed rows only. Verified rollback after a second award batch fails, rollback of preference deletion, success counts, and HTTP 500 instead of success for an injected transaction failure. Tests use session-local tables and private sequences, not application records. TypeScript, lint, 245 bid checks, local browser startup, and smoke passed.

5. **Complete — date-aware award identity.** Preserve repeated trips on different operating dates, normalize stable employee identity and date formats, and serialize imports for the same report with a transaction advisory lock. Verified distinct-date preservation, duplicate/re-import idempotency, and exclusion of a competing database session; TypeScript, lint, 245 bid checks, local browser startup, and smoke passed.

6. **Complete — preserve saved work on upload.** Removed automatic deletion of earlier package versions. Favorites, calendar entries, chat, and history links remain attached to the original package, available in the existing selector; only explicit deletion removes a package. Upload feedback explains retention. Verified the actual upload handler against an existing same-period package with saved references; TypeScript, lint, bid checks, browser startup, and smoke passed.

7. **Complete in code — authenticated access and PIN storage.** Added a deployment password gate across server routes, browser sign-in entry for separately hosted frontend assets, cross-origin write rejection, and canonical-user ownership validation. PIN writes use salted scrypt and API responses omit credentials. Four regression tests cover access, ownership, hash verification, and an idempotent migration against temporary tables. TypeScript, lint, bid checks, build, browser startup, and smoke passed. Hosted activation requires the coordinated password/origin configuration and PIN migration in `docs/access-setup.md`; no live deployment or credential migration was performed.

8. **Complete — vulnerable dependencies.** Updated runtime/build dependencies and scoped vulnerable transitive overrides. Both full and production-only npm audits report zero vulnerabilities. Preserved database recovery through Drizzle's new error wrapper. Verified 13 regressions, bid checks, TypeScript, lint, frontend/server bundles, temporary schema generation, browser startup, and smoke. Override rationale is in `docs/dependency-overrides.md`.

9. **Complete — coach preference identity.** The coach endpoint resolves the canonical profile server-side, supplying its ID and current seniority even when an older client omits userId. A route regression confirms omitted identity and stale client seniority cannot select neutral/wrong profile context; zero percentile is preserved. TypeScript, lint, bid checks, browser startup, and smoke passed. Paid model calls were stubbed in regression tests.

10. **Complete — consistent duration units and source-based repair.** Shared minute conversion now handles Reasons credit/fingerprints, PDF totals, TAFB SQL/client filtering and sorting, and table display. Re-imports repair existing credits atomically while preserving links and unrelated fingerprint fields. Verified printed/decimal distinctions, SQL/client agreement, idempotent correction and rollback, bid checks, TypeScript, lint, build, browser startup, and smoke. Historical repair requires original Reasons HTML; none was found locally, so stored history was not guessed or rewritten. See `docs/duration-data.md`.

11. **Complete — collision-free cache keys.** Replaced truncated Base64 with complete canonical filter serialization, including nested objects and Unicode. Versioned keys isolate old cache entries; profile/history revisions remain in the namespace. Two regression tests cover the reproduced long-prefix collision, equivalent ordering, distinct packages/profiles, and old-key invalidation. TypeScript, lint, bid checks, browser startup, and smoke passed.

12. **Complete — one complete dataset and reliable offline caching.** Removed the 400-row heuristic, paginated prefetch assumption, duplicate filtered/unfiltered fetches, and redundant completion refetches. Added a compact complete-dataset endpoint, isolated query keys, shared local filtering, cache completeness checks, offline metadata fallback, and safe IndexedDB connection/purge handling. The live 236-row package response is 44% smaller; synthetic browser tests confirmed one initial dataset request, local filter/sort changes without re-fetching, three-row package switching, cached error/offline recovery, and raw detail loading on demand. All 23 regression tests, 245 bid checks, TypeScript, lint, build, and smoke passed; npm audit remains at zero. Details and reproducible browser fixture are in `docs/dataset-verification.md`.

### Testing access: four-digit PIN

Replaced the hosted login flow with a PIN-only form when `APP_ACCESS_PIN` is configured. Local development remains login-free. Random seven-day session cookies are backed by hashed tokens in PostgreSQL, and a shared five-attempt/15-minute budget survives serverless restarts. Existing password deployments remain compatible until rollout. Added migration 004 and its setup script; no hosted settings or database tables were changed.

Validation: six access, session, and ownership tests passed, including temporary-table database tests for shared budgets and expiry. Type-check, lint, client build, server bundle, and local API smoke checks passed. Hosted setup instructions are in `docs/access-setup.md`.
